node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env*.local

.DS_Store
Thumbs.db

.vercel
coverage/
*.log
npm-debug.log*
.cache/

# 外部から落とした生データの置き場。いずれも取り直せる一時キャッシュで、
# 成果物は data/computed/ に入る。
# 標高は駅ごとの最小・最大・標高差を terrain.json が持つので、
# 点ごとの値を残す必要はない（国土地理院への問い合わせを減らすためだけのもの）。
data/poi/
__pycache__/
