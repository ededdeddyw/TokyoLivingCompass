# スコア設計

構想 §6「東京の駅偏差値」の実装仕様。

---

## 1. 原則

- 全軸 **0–100 の整数**。50 が「東京の住宅地として平均的」。
- 単一の総合ランキングは作らない。**重みはユーザー側が決める**（構想 §6）。
- 表示は ◎○△× に丸めるが、内部は数値で保持する。
- スコアは「東京の住宅地の中での相対評価」であり、全国比較ではない。

---

## 2. 16 の評価軸

| キー | 日本語 | 定義（何を測っているか） |
|---|---|---|
| `rentValue` | 家賃コスパ | 同等の利便性を持つ駅と比べた家賃の安さ |
| `commute` | 都心アクセス | 主要オフィス街への到達しやすさ |
| `transitConvenience` | 乗換利便性 | 路線数、直通先の多さ、始発の有無 |
| `shopping` | 買い物 | スーパー・コンビニ・日用品の充実度 |
| `food` | 飲食店 | 外食の選択肢の多さと質 |
| `cafe` | カフェ | 作業できるカフェ・喫茶の充実度 |
| `nightlife` | ナイトライフ | 夜間営業の店・バーの多さ |
| `safety` | 治安 | 犯罪発生状況と夜道の歩きやすさ |
| `quietness` | 静かさ | 騒音・人通り・繁華街からの距離 |
| `family` | ファミリー適性 | 学校、公園、小児科、ベビーカー移動のしやすさ |
| `singleLife` | 一人暮らし適性 | 単身向け物件の量、生活の完結度 |
| `internationalFriendliness` | 外国人生活適性 | 外国語対応、外国人居住者の多さ、入居のしやすさ |
| `style` | 街のおしゃれさ | 街並みの雰囲気、店の質感 |
| `nature` | 公園・自然 | 公園・緑地・河川の充実度 |
| `healthcare` | 病院 | 医療機関の数と規模 |
| `fitness` | ジム | ジム・スポーツ施設の充実度 |

軸の追加・削除はスキーマ変更を伴うため、`src/lib/schema.ts` の `SCORE_AXES` を単一の正とする。

---

## 3. ◎○△× への変換

| スコア | グレード | 意味 |
|---|---|---|
| 80–100 | ◎ | この駅の強み |
| 60–79 | ○ | 十分に良い |
| 40–59 | △ | 平均的 |
| 0–39 | × | 弱点 |

アクセシビリティのため、記号だけでなくテキストラベル（`excellent` / `good` / `fair` / `poor`）を併記する。

---

## 4. スコアの付け方

現行のシードデータは編集者の判断による暫定値。公開版では以下の混成とする。

| 軸 | 一次データ | 人間の判断 |
|---|---|---|
| `rentValue` | 家賃相場と通勤時間から算出 | 補正のみ |
| `commute` `transitConvenience` | 所要時間・路線数から算出 | 補正のみ |
| `shopping` `food` `cafe` `nightlife` `healthcare` `fitness` | 施設数（POI カウント） | 質の補正 |
| `safety` | 警視庁の犯罪発生マップ | 体感の補正 |
| `nature` | 公園面積・緑被率 | 補正のみ |
| `quietness` `style` `family` `singleLife` `internationalFriendliness` | 参考値のみ | **主に人間の判断** |

つまり「機械で出せる軸は機械で出し、土地勘が要る軸に人間を集中させる」。これが構想 §4.2 の差別化を運用可能にする形。

> 注意: 現時点のリポジトリのスコアは全て `dataQuality: "seed"` の推定値であり、上記の一次データにはまだ紐づいていない。

---

## 5. 重みプリセット

`src/lib/weights.ts` に定義。重みの合計は 1.0 に正規化される。

| プリセット | 重視する軸 |
|---|---|
| `balanced` | 全軸をゆるやかに配分（既定） |
| `single` | 通勤・飲食店・家賃コスパ・一人暮らし適性 |
| `family` | 治安・静かさ・ファミリー適性・買い物・公園 |
| `quiet` | 静かさ・治安・公園・家賃コスパ |
| `value` | 家賃コスパ・通勤・買い物 |
| `international` | 外国人生活適性・通勤・乗換利便性・飲食店 |

総合評価（0–10）= Σ(score × weight) / 10。

---

## 6. 適合度（逆引き検索）

勤務先起点検索の適合度は、スコアだけでなく**制約の充足**を含める。

```
適合度 = 制約フィルタ（通過 / 除外）× 重みつきスコア × 通勤ボーナス
```

1. **制約フィルタ**（満たさない駅は候補から除外）
   - 指定間取りの家賃相場 ≤ 家賃上限
   - 勤務先までの所要時間 ≤ 上限
   - 乗換回数 ≤ 上限
2. **重みつきスコア**: 選択した重みプリセット、または個別指定の重み。
3. **通勤ボーナス**: 上限に対して所要時間が短いほど加点（上限ぎりぎりより余裕がある方を上位に）。

出力には必ず「なぜ推すのか（高スコア軸）」「弱点（低スコア軸）」「似た候補」を添える（構想 §7）。
