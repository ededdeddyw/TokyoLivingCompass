# Tokyo Living Compass

東京の駅ごとの住みやすさを多言語で提供し、「東京でどこに住むべきか」を決めるための
意思決定エンジン。物件を探す**前に**、住む街を決めるためのサービス。

- 事業構想: [docs/00-concept.md](./docs/00-concept.md)
- 駅ページの品質基準: [docs/12-quality-standard.md](./docs/12-quality-standard.md)
- 全443駅の計画: [docs/11-all-stations-plan.md](./docs/11-all-stations-plan.md)

現在の方針: **まず日本語で作り切る。多言語展開はそのあと。**
既存の不動産まとめ記事に対する優位は「深さ」でしか作れず、浅い記事を13言語に
翻訳しても勝ち筋にならないため。

---

# 日本語表現ルール（最重要・常時適用）

**本リポジトリで生成するすべての日本語に、以下のルールを必ず適用する。**
駅コンテンツ（`data/content/ja/`）、ドキュメント、コミットメッセージ、UI 文言、
チャットでの報告文を含む、例外なくすべて。

全文と NG/OK 例は [docs/13-japanese-style-rules.md](./docs/13-japanese-style-rules.md)。
判断に迷ったら必ず全文を読むこと。以下はその要約であり、全文が正である。

**大原則: 本文・見出し・表・注記など、アウトプットに含まれるすべての日本語に適用する。
表や箇条書きだからといって目的語や主語を省略しない。**

| # | ルール |
|---|---|
| 1 | 人間が普通使わない不自然な表現・造語的な圧縮表現を使わない。平易な言葉に開く |
| 2 | 問題提起と解決策の間で論理矛盾を起こさない。課題側は「現状の仕組みでは」と限定する |
| 3 | 文末の調子を揃える。体言止め・である調・ですます調を混在させない |
| 4 | 目的語・主語を省略しない。「統制を確立」のような抽象語で逃げない |
| 5 | 省略しすぎない。「誰の」「何からの」「何のための」を補う。簡潔さより伝わることを優先する |
| 6 | 語順とつなぎの言葉にこだわる。時間・状況を表す語は文頭側に置き、目的語と述語を離さない |
| 7 | 「〜できる状態にする」「〜を実現する」等の冗長な述語を削り、端的な動詞で言い切る |
| 8 | 誰が何をどうしてどうなるかが目に浮かぶ文にする。動作の主体を明示する |
| 9 | 指示対象を明示する。意図不明の付随情報は削る |
| 10 | 結論・効果は、それまでの論旨で主張してきたことを主役に据える。新概念を主役にしない |
| 11 | 省略しないことを既定路線にする。迷ったら書く |
| 12 | 前後の文脈と呼応する言葉を選ぶ。言い換えで範囲を狭めない（矮小化しない） |
| 13 | 主張の強さが手段の実力を超える表現を避ける。断定しなくても文章は弱くならない |
| 14 | 読み手に伝わらない社内参照・唐突な情報は消すか、判断がつかなければ確認する |
| 15 | 並列する項目の粒度を揃える。施策とその各論を同列に並べない |
| 16 | 出来事を名詞だけで圧縮せず、動詞を補う |
| 17 | 省略後に多義的になる単語（「評価」等）は省略しない |
| 18 | 誰にでも伝わるとは限らない独自ワード・複合語を作らない |
| 19 | 事実と評価語を混同した単語を使わない |
| 20 | 仮定の行動を前提にした課題を立てない。実在する事実から立てる |
| 21 | 課題との対応を示す見出しには、課題に対応する内容だけを書く |
| 22 | 同じ述語を連発しない。文末を散らす |
| 23 | 冒頭に初出のツール名・製品名が来るときは、何者かの説明を補う |
| 24 | 目的を先に置いて文の論理を通す。後出しにしない |
| 25 | 課題と対応が同語反復にならないようにする |
| 26 | 単独の「抜け」は使わず、常に「抜け漏れ」と書く |

## 機械チェック

`npm run check:ja` が、機械的に判定できる違反（文末の混在、強すぎる断定、
冗長な述語、同じ述語の連発、単独の「抜け」など）を検出する。
**ただし機械が見られるのは一部にすぎない。**ルール2・5・10・12・15・20・25 のような
論旨に関わるものは検出できないため、書いたあとに全文を読み返して自分で確認する。

---

# 開発

```bash
npm run dev              # 開発サーバー
npm run validate:data    # データ検証と充足率レポート
npm run check:ja         # 日本語表現ルールの機械チェック
npm run typecheck
npm run build
```

データ生成（いずれも標準ライブラリのみで動く）:

```bash
python3 scripts/build-roster.py     # 全443駅と路線マスタ
python3 scripts/build-commutes.py   # 所要時間（443駅 × 7オフィス街）
python3 scripts/build-scores.py     # 計算で出せるスコア軸
```

## 設計上の約束

1. `data/` を読むのは `src/lib/stations.ts` だけ。
2. `src/lib/schema.ts` がデータ定義の単一の正。
3. コンポーネントはデータを取りに行かない。props で受け取る。
4. 派生値（総合評価、グレード、適合度）は保存せず、毎回計算する。
5. 駅データは「ロースター（事実）＋計算値＋プロフィール」の重ね合わせ。
   家賃もスコアも軸ごとに任意で、揃った順に埋めていく。
